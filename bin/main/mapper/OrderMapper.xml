<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rollback.repository.OrderRepository">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (customer_name, amount, status)
        VALUES (#{customerName}, #{amount}, #{status})
    </insert>

    <select id="findById" resultType="com.example.rollback.domain.Order">
        SELECT id, customer_name, amount, status
        FROM orders
        WHERE id = #{id}
    </select>

    <select id="findAll" resultType="com.example.rollback.domain.Order">
        SELECT id, customer_name, amount, status
        FROM orders
        ORDER BY id DESC
    </select>

    <update id="updateStatus">
        UPDATE orders
        SET status = #{status}
        WHERE id = #{id}
    </update>

</mapper>