<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rollback.repository.AccountRepository">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO accounts (account_number, customer_id, account_type, currency, balance, overdraft_limit, status, account_holder_name, branch_code)
        VALUES (#{accountNumber}, #{customerId}, #{accountType}, #{currency}, #{balance}, #{overdraftLimit}, #{status}, #{accountHolderName}, #{branchCode})
    </insert>

    <select id="findById" resultType="com.example.rollback.domain.Account">
        SELECT id, account_number as accountNumber, customer_id as customerId, account_type as accountType, 
               currency, balance, overdraft_limit as overdraftLimit, status, 
               account_holder_name as accountHolderName, branch_code as branchCode,
               created_at as createdAt, updated_at as updatedAt, last_transaction_at as lastTransactionAt
        FROM accounts
        WHERE id = #{id}
    </select>

    <select id="findByAccountNumber" resultType="com.example.rollback.domain.Account">
        SELECT id, account_number as accountNumber, customer_id as customerId, account_type as accountType, 
               currency, balance, overdraft_limit as overdraftLimit, status, 
               account_holder_name as accountHolderName, branch_code as branchCode,
               created_at as createdAt, updated_at as updatedAt, last_transaction_at as lastTransactionAt
        FROM accounts
        WHERE account_number = #{accountNumber}
    </select>

    <select id="findByCustomerId" resultType="com.example.rollback.domain.Account">
        SELECT id, account_number as accountNumber, customer_id as customerId, account_type as accountType, 
               currency, balance, overdraft_limit as overdraftLimit, status, 
               account_holder_name as accountHolderName, branch_code as branchCode,
               created_at as createdAt, updated_at as updatedAt, last_transaction_at as lastTransactionAt
        FROM accounts
        WHERE customer_id = #{customerId}
        ORDER BY id DESC
    </select>

    <select id="findAll" resultType="com.example.rollback.domain.Account">
        SELECT id, account_number as accountNumber, customer_id as customerId, account_type as accountType, 
               currency, balance, overdraft_limit as overdraftLimit, status, 
               account_holder_name as accountHolderName, branch_code as branchCode,
               created_at as createdAt, updated_at as updatedAt, last_transaction_at as lastTransactionAt
        FROM accounts
        ORDER BY id DESC
    </select>

    <update id="updateBalance">
        UPDATE accounts
        SET balance = #{balance}, 
            updated_at = CURRENT_TIMESTAMP,
            last_transaction_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE accounts
        SET status = #{status}, updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="update">
        UPDATE accounts
        SET account_type = #{accountType},
            currency = #{currency},
            balance = #{balance},
            overdraft_limit = #{overdraftLimit},
            status = #{status},
            account_holder_name = #{accountHolderName},
            branch_code = #{branchCode},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM accounts WHERE id = #{id}
    </delete>

</mapper>